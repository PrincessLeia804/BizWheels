<!DOCTYPE html>
<html lang="en">

<head>
  <meta charset="UTF-8" />
  <meta name="viewport" content="width=device-width, initial-scale=1.0" />
  <title>BizWheels</title>
  <link rel="shortcut icon" type="image/png" href="/images/sports-car.png" />
  <link rel="stylesheet" href="/stylesheets/style.css" />
  <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css" />
</head>

<body>
  <nav class="navbar navbar-expand-lg navbar-dark bg-dark">
    <div class="container">
      <a class="navbar-brand" href="/">BizWheels</a>
      <button class="navbar-toggler" type="button" data-bs-toggle="collapse" data-bs-target="#navbarNav" aria-controls="navbarNav" aria-expanded="false" aria-label="Toggle navigation">
        <span class="navbar-toggler-icon"></span>
      </button>
      <div class="collapse navbar-collapse" id="navbarNav">
        <ul class="navbar-nav ml-auto">
          <li class="nav-item">
            <a class="nav-link" href="/">Home</a>
          </li>
          <% if (session.user) { %> <% if (session.user.role === "admin") { %>
          <li class="nav-item">
            <a class="nav-link" href="/admin">Dashboard</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/admin/fleet">Car management</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/admin/users">User management</a>
          </li>
          <% } else { %>
          <li class="nav-item">
            <a class="nav-link" href="/profile">Dashboard</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/profile/my-account">My account</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/cars/request">Car request</a>
          </li>
          <li class="nav-item">
            <a class="nav-link" href="/cars/reservations">My reservations</a>
          </li>
          <% } %> <% } %>
        </ul>
      </div>
      <span class="navbar-text">
        <% if (session.user) { %>
        <div class="userStatus">
          <p>Hi <%-session.user.firstName%> <%-session.user.lastName%>!</p>
          <a href="/auth/logout" class="btn btn-outline-light">Logout</a>
        </div>
        <% } else { %>
        <div class="userStatus">
          <a href="/auth/login" class="btn btn-outline-light">Login</a>
        </div>
        <% } %>
      </span>
    </div>
  </nav>

  <%-body%>
  <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>

</html>